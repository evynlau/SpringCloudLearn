server:
  port: 8889

spring:
  application:
    name: config-client
  cloud:
    config:
      label: config
      name: config
      profile: dev
      uri: http://localhost:8888
  kafka:
    bootstrap-servers: 192.168.47.129:9092,192.168.47.130:9092,192.168.47.131:9092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

#暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"